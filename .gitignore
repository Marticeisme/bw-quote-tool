node_modules/
scratch/
wmp-cemetery-map/
# source material kept local-only: internal/partner PDFs are inputs to the
# published guides, never published themselves (Martice, 2026-07-28)
reference-docs/internal/
pdf-assets/Needs Bonney Watson Branding/
pdf-assets/Urn Options Quote 06.25.26/
# written and deleted by tests/test-price-update-path.mjs; never a real page
index.prices-test.html
# PCM single design proofs: generated by scripts/pcm_single_import.py, held out of the
# public repo pending Martice's own decision on the PII census in that script's
# PII_POSTURE. Regenerate locally in one command; delete these two lines to publish.
pcm-single-images/
data/pcm-single-proofs.json
