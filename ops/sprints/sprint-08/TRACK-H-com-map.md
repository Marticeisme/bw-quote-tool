# TRACK H — Chapel of Memories mausoleum 3D crypt map

Repo: `C:\Users\Martice\bw-quote-tool`; you run in the WORKTREE
`C:\Users\Martice\bw-quote-tool-com` on branch `s08/com-map` (node_modules junction in
place). ALWAYS `git -C C:\Users\Martice\bw-quote-tool-com …`. Obey
`ops/SPRINT_GUIDELINES.md` + `ops/DESIGN.md`. Commit locally with explicit paths, NEVER
push. Never write to production Firebase. **Never read or reference `wmp-cemetery-map/`**
— absent from your worktree by design and its data may not cross into this public repo
in any form (DESIGN §6).

## Goal

`MAPS/COM_CryptMap.html` — an interior 3D map of the Chapel of Memory Mausoleum —
GENERATED by NEW `scripts/build_com_map.mjs` from NEW `scripts/com-crypt-data.mjs`,
gated by NEW `scripts/verify_com_map.mjs`. Fourth member of the map family; read
`scripts/build_ecl_map.mjs` and `scripts/build_roac_map.mjs` first and reuse their
interaction stack (tap detector, deferred capture, frozen hover mid-drag,
pointer-transparent cards, card-beside/bottom-sheet, print = static flat grids, print
follows highlight, "← Quote Tool" back button, exact prices never rounded, status =
pattern never hue). ROAC is the closest shape: an interior courtyard the camera sits
inside. COM is bigger: this is a walk-in building.

## Sources — `D:\Cemetery Photos Misc\Chapel of Memories\`

1. **`COM Maus Crypts.png` — THE authoritative price/status sheet.** Title: "CHAPEL OF
   MEMORY MAUSOLEUM - CRYPTS", "OFFICIAL PROPERTY ADDRESS: COM-1-1-ROW-SPACE".
   Column numbers 101–231 group into banks with headers (SINGLES, TRUE COMPANIONS
   (TANDEMS), DELUXE COMPANIONS, HIDDEN COMPANIONS, and combinations); tiers G (top)
   to A (bottom). Transcribe EVERY cell: space number, tier, bank/type, price if
   printed, "NOT SELLING" if printed, and the two "EMPTY AREA — NO CRYPTS IN THIS
   SECTION" voids. Zoom into regions (crop with Python/PIL) — transcribe from crops,
   never from a squint at the full sheet.
2. **`Chapel Of Memories Overview.png`** — CAD floor plan: where every column number
   sits in the building, plus CHAPEL AREA, ALTAR, REST ROOMS, STORAGE ROOM, ENTRANCE,
   hallways, RADIANCE NICHES and SERENITY NICHES locations.
3. **`CHapel of Memory Maus.png`** — color plan with per-column type letters
   (T/S/D/C/H) and the "Niches" / "Tiers E–G only" legend. Cross-check types against
   the crypts sheet's bank headers; where they disagree, the CRYPTS sheet wins and the
   discrepancy goes in your report.
4. ~30 photos + `20260729_124129.mp4` (operator walking the whole building). No ffmpeg
   on PATH. If you can extract frames cheaply (e.g. `npx` ffmpeg static, or Playwright
   playing the file and screenshotting `<video>` seeks), do it for geometry/material
   reference; if that costs more than 20 minutes, use the photos alone and say so.
   Materials/proportions are ESTIMATED — say so in the header; NO dimensions rendered.

## Operator rulings (2026-07-29, binding)

- Printed price ⇒ AVAILABLE. "NOT SELLING" ⇒ blocked (own status + badge, never
  sellable). **Everything else ⇒ UNAVAILABLE** — badge/wording "Unavailable — confirm
  in MIS", no claim of sold vs reserved. Cell COLORS and dotted marks carry no meaning.
- No price rendered anywhere for a non-available crypt; `p: null` for all of them.
- Fee box: transcribe ONLY legible figures (Recording Fee $225.00, Vase $415.00,
  Monobar Install Fee $215.00 visible; several rows are `########` — Open & Closing and
  Monobar each appear obscured). **An illegible figure is OMITTED from card math and
  listed in your report** — never invent, never reuse another building's number.
  E.C.F. 10%, not included in listed prices.
- Card math: price + 10% ECF + whichever fees you can legitimately include; type
  (Single / True Companion (Tandem) / Deluxe Companion / Hidden Companion) shown on the
  card. Tandems/companions are one purchasable unit at one price — never split.
- **Radiance and Serenity niche walls are FULL INVENTORY this sprint** (upgraded
  mid-boot when the operator supplied their sheets). Sources in
  `D:\Cemetery Photos Misc\Radiance and Serenity Niches\`:
  `Radiance.png` — "CHAPEL OF MEMORY 'Radiance' Niche Wall", RAD-1-1-ROW-SPACE, rows
  K (top) → A (bottom), columns 1–8 with merged/variable cells; some cells carry
  prices; size classes Family(2) 11⅞"×30½"×25½", X-Large(2) 11⅞"×26"×12¾", Large(2)
  11⅞"×23"×12¾", Small(2) 11⅞"×18¼"×12¾"; PRICES EFFECTIVE JANUARY 13, 2025; O&C
  $835 ea, Recording $225 ea, E.C.F. 10% not included.
  `Serenity.png` — SER-1-1-ROW-SPACE, rows K → A, columns 1–6; Large(2)
  10½"×22⅛"×12¾", Small(2) 10½"×11 1/16"×12¾"; same fees.
  Same fail-safe reading as the crypts: **price ⇒ AVAILABLE; anything else ⇒
  UNAVAILABLE ("confirm in MIS"); colors carry no meaning.** Refs
  `RAD-1-1-<row>-<col>` / `SER-1-1-<row>-<col>`. These sheets DO print dimensions —
  show the size-class table as a per-wall legend; attach a size class to an individual
  niche ONLY where the sheet makes it unambiguous (cell span/row structure), otherwise
  legend-only and say so in the report. Place both walls at their floor-plan locations;
  they join the gate: counts, price multisets, available-$ anchors, no price on
  unavailable, determinism, sabotage. Also transcribe the effective date onto the page
  footer for the walls. Three photos in that folder + several among the COM photos show
  the real installations — match materials from them.
- **PII, absolute:** the photos and video show real occupant names on crypt fronts.
  NO name, initial, date, or legible plate text may enter the repo — not in code,
  comments, fixtures, screenshots you commit, or the report. Textures are procedural,
  never photo-derived. (Same rule that governs the map repo; this repo is PUBLIC.)

## Presentation (your latitude, within the family's conventions)

The building has banks on perimeter walls, two center islands, and side rooms — a
single orbit view cannot show it all. You own the design; requirements: a floor-plan
overview (SVG, from the CAD plan) where every bank is clickable; per-bank 3D or face-on
views with the family's interaction stack; flat printable grids per bank; the chapel/
altar/hallway/entrance masses present so the operator can orient. Tabs may group banks
by area rather than one tab per bank (there are ~25 banks — one tab each is unusable).

## `guides.html`

Append ONE card in the Maps category (after the ECL card, matching markup), pill 5 → 6.
Touch nothing else.

## Verification gates (quote outputs verbatim)

1. `scripts/verify_com_map.mjs`: build determinism; per-bank and per-column crypt
   counts vs your transcription table; total crypts / available count / available-$
   anchors; zero price strings rendered for unavailable or blocked crypts; every ref
   unique, format `COM-1-1-<tier>-<space>`; the two EMPTY-AREA voids contain zero
   crypts; sabotage-proven (≥3 sabotages incl. a price moved to another valid tier —
   the anchor-total must catch it; all exit 1, restored).
2. Playwright interaction suite in `scratch/`: tap/touch select, drag ≠ select, area
   navigation, print scope, back button, no money on unavailable/blocked cards.
3. `npm run check` 8/0; `npm test` ≥ `1327 passed, 0 failed across 27 suites`, never
   falling; `node scripts/verify_guides_page.mjs` ALL OK.
4. Screenshots (overview + each area + cards for one available, one unavailable, one
   NOT SELLING) — render and LOOK (ops/MISTAKES.md: counting is necessary, not
   sufficient). Confirm no committed screenshot shows a real name.
5. Your report carries the FULL transcription table (bank → column → tier grid with
   price/status) so the director can spot-check against the sheet.

## Report

What shipped; branch + commits; verbatim gate outputs; transcription table; fee rows
omitted as illegible; type-letter discrepancies vs the crypts sheet; decisions & open
questions; screenshot list; what only the operator can verify (he walks this building).
