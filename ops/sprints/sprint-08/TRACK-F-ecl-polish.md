# TRACK F — ECL map polish: champagne materials + add-on toggles

Repo: `C:\Users\Martice\bw-quote-tool`; you run in the WORKTREE
`C:\Users\Martice\bw-quote-tool-eclp` on branch `s08/ecl-polish` (node_modules junction
in place). ALWAYS `git -C C:\Users\Martice\bw-quote-tool-eclp …`. Obey
`ops/SPRINT_GUIDELINES.md` + `ops/DESIGN.md`. Commit locally with explicit paths, NEVER
push. Never write to production Firebase.

The ECL map shipped LIVE today (sprint-07): `MAPS/ECL_NicheMap.html`, generated by
`scripts/build_ecl_map.mjs` from `scripts/ecl-niche-data.mjs`, gated by
`scripts/verify_ecl_map.mjs`. Never hand-edit the HTML — edit builder/data, rebuild.

## Operator feedback (2026-07-29, binding)

1. **"Black does not look good here."** The niches currently render as near-black
   glass. Ruling: go **champagne-lit like MVC** — read the material treatment in
   `scripts/build_mvc_map.mjs` (warm champagne-gold lit niche interiors, near-black
   bronze frame, brass trim) and apply the same family of materials to ECL's glass
   fronts, tuned against the ECL photos
   (`D:\Cemetery Photos Misc\Eternal Light Columbarium (NEW)\*.jpg`).
   **Sold niches become a dimmed/frosted champagne with the white SOLD badge — NOT
   solid black.** Sold/available must stay unmistakably distinct at a glance
   (pattern/brightness, never hue alone), and sold niches still render NO price
   anywhere (the gate already asserts this — it must stay PASS).
2. **Bronze Scroll ($785) and Vase with Ring ($370) must be toggleable** into the
   detail-card math. Today they sit in the fee footer as text-only add-ons. Add them
   to the pinned/hover detail card as opt-in toggles (default OFF), alongside the
   existing O&C and Recording qty inputs; when toggled on, the card total includes
   them (they are NOT subject to E.C.F. — E.C.F. is 10% of the niche price only,
   matching how the card computes today). The printed pricing card for a highlighted
   niche reflects the toggle state. Mirror the fee-footer toggles' look-and-feel from
   whatever the card already does for O&C/Recording quantities.

## Hard constraints

- DATA DOES NOT CHANGE: 85 niches, 28 available, $685,175 list, statuses as-is
  (N-B-2 is sold — operator ruling earlier today). `verify_ecl_map.mjs` must stay
  PASS with those same anchors. If you touch the verify script at all, it is only to
  ADD assertions (e.g., toggle math), never to relax any.
- Price-chip contrast: keep every tier chip label ≥ 4.5:1 WCAG AA against its fill
  (the tier palette itself may stay; it's the niche GLASS/frame materials that go
  champagne). If you adjust any chip colors, re-measure and quote the ratios.
- Print output stays JS-free static HTML; a highlighted niche's printed card shows
  the toggled fees only if toggled at print time.
- Do not touch `index.html`, `guides.html`, other maps, or anything else.

## Verification gates (quote outputs verbatim)

1. `node scripts/verify_ecl_map.mjs` → PASS, 0 mismatches, same anchors
   (85 / 28 / $685,175; 57 sold + 0 unpriced).
2. New assertions (yours): toggling Bronze Scroll changes the card total by exactly
   +$785, Vase by +$370, both off = today's total; E.C.F. unaffected by toggles.
3. Playwright interaction suite in `scratch/` (rewrite from the s07 patterns — the old
   copy died with its worktree): tap/touch select, drag ≠ select, presets fit, print
   scope, toggle math on a pinned card, print card reflects toggles, zero page errors.
4. `npm run check` → `index.html: 8 blocks, 0 errors`; `npm test` ≥
   `1327 passed, 0 failed across 27 suites`, never falling.
5. Screenshots: room view + one face + a pinned available card (toggles off/on) + a
   sold niche — LOOK at them; the operator's complaint was aesthetic, so the fix is
   judged by eye as well as by gate. Include a before/after pair of the same view.

## Report

What shipped; branch + commits; verbatim gate outputs; WCAG measurements if chips
changed; screenshots list (before/after); decisions & open questions.
