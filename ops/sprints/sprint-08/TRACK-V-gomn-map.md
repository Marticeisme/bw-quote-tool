# TRACK V — Garden of Meditation niche map (2D)

Repo: `C:\Users\Martice\bw-quote-tool`; you run in the WORKTREE
`C:\Users\Martice\bw-quote-tool-gomn` on branch `s08/gomn-map` (node_modules junction
in place). ALWAYS `git -C C:\Users\Martice\bw-quote-tool-gomn …`. Obey
`ops/SPRINT_GUIDELINES.md` + `ops/DESIGN.md`. Commit locally with explicit paths,
NEVER push. Never write to production Firebase; never read `wmp-cemetery-map/`.

## Goal

`MAPS/GOMN_NicheMap.html` — the Garden of Meditation niche wall. **Operator: 2D is
fine.** No 3D scene: an accurate stepped-wall 2D rendering (the flat-grid style the 3D
maps use for their wall tabs and print output), with the family's card/selection/print
conventions. GENERATED by NEW `scripts/build_gomn_map.mjs` from NEW
`scripts/gomn-niche-data.mjs`, gated by NEW `scripts/verify_gomn_map.mjs` — never
hand-edit the HTML. Read `scripts/build_ecl_map.mjs` first and reuse its flat-grid
markup, hover/pin card, tap detector (2D still needs touch-friendly selection),
print-follows-highlight, and "← Quote Tool" header. Statuses are live hand-maintained
data — say so at the top of the data module.

## Sources — `D:\Cemetery Photos Misc\GOMN Niches\`

- **`GOMN MAP.png` — THE authoritative price sheet.** "NEW PROPERTY PRICES EFFECTIVE
  JANUARY 30, 2025". OFFICIAL PROPERTY ADDRESS **GOM-1-1-ROW-SPACE**. Three blocks:
  left wing cols 1–8 (rows A–B full width; a raised block rows C–E on cols 5–8),
  GARDEN OF MEDITATION - CENTER SECTION cols 9–24 (rows A–G), right wing cols 25–32
  (rows A–B full; raised block rows C–E on cols 25–28). Transcribe EVERY cell from
  crops (PIL), never from a squint. Fail-safe reading (the operator's standing rule
  today): **printed price ⇒ AVAILABLE; anything else ⇒ UNAVAILABLE ("confirm in
  MIS"); cell colors carry no meaning.** `p: null` for every unavailable niche.
- **`GOMN NICHE MAP.png`** — the wall outline confirming the stepped shape and
  row/column extents. Cross-check every block's row/col range against the price
  sheet; discrepancies go in the report, the PRICE sheet wins.
- `Garden of Meditation01-front.jpeg` + two 2026-07-27 photos — appearance reference
  for header/colors only (it is an outdoor garden wall). If a photo shows legible
  plate names, nothing from it may be transcribed; no photo ships on the page unless
  you add one AND verify plates are illegible (a photo is optional here — the map is
  the deliverable).

## Sheet rules — carry them onto the page (rules footer + card)

- E.C.F. 10%, not included in listed prices; Open & Closing $835 ea; Recording Fee
  $225 ea; **Inscription $605 ea**.
- **All niches are sold as Companions (2) and include one niche vase** — state on the
  page and in every card; card math: price + 10% ECF + O&C + Recording (+ optional
  Inscription toggle, default off, matching the ECL toggle pattern Track F just
  landed if it has merged by your branch point — check `git log`; if F hasn't merged,
  a plain listed line is fine and say so).
- **"ONLY the Interlude Urn is allowed in these Niches due to size. Refer to URN
  price list for price."** — carry verbatim (it's a sales-critical constraint).
- **"NO PHOTOS ALLOWED"** — carry verbatim in the rules footer. Do not reinterpret it;
  quote the sheet.
- Effective date on the page footer.

## `guides.html`

Append ONE card at the END of the Maps category, matching markup, pill +1 from
whatever you find. NOTE: Track H also appends a Maps card on its own branch; the
director resolves the adjacent-card/pill conflict at merge — keep your change to
exactly one card + the pill number.

## Verification gates (quote outputs verbatim)

1. `scripts/verify_gomn_map.mjs`: build determinism; per-block counts (state them
   from your transcription; the sheet prints no COUNT fields, so your gate carries
   its own transcription-derived anchors: total niches, available count, available-$
   total, price multiset); zero price strings rendered for unavailable niches; refs
   unique and `GOM-1-1-<row>-<col>`; the stepped voids (e.g. rows C–G outside their
   blocks) contain NO niches; sabotage-proven (≥3, incl. a price moved to another
   valid tier caught by the $-anchor; all exit 1, restored).
2. Playwright suite in `scratch/`: tap/touch select, card opens, print scope follows
   highlight/selection, back button, no money on unavailable cards, zero page errors.
3. `npm run check` 8/0; `npm test` counts never fall from branch time;
   `node scripts/verify_guides_page.mjs` ALL OK (with your card).
4. Screenshots: full wall + one available card + one unavailable card — LOOK at them.
5. Report carries the FULL transcription table for director spot-check.

## Report

What shipped; branch + commits; verbatim gates; transcription table; decisions & open
questions; screenshots list.
