# TRACK D — s21/lightbox-print — print a single design from the enlarged view

Operator round, 2026-08-07, his words: "When enlarging a single pcm image there needs to
be an option to print that. Right now there is no print option for a single marker the
same way you can on the casket catalogs."

You are an Opus track agent. Worktree `C:\Users\Martice\bw-quote-tool-s21d`, branch
`s21/lightbox-print`. Stale-base check first: branch must contain local main's tip
(`git -C C:\Users\Martice\bw-quote-tool rev-parse main`). `git -C` absolute paths,
explicit-path staging, never touch index.html (sha256 before/after proof) or any guide.

**pcm-design-catalog.html is GENERATED by `scripts/build_pcm_catalog.py`** — edit the
builder, rebuild (twice, byte-identical), never hand-edit the page.

## The job

1. Study BOTH sides first: (a) how the PCM catalog's enlarge/lightbox works today
   (find it in the builder), and (b) how the casket catalogs do single-item print —
   read one casket catalog page's print machinery AND its gates
   (`scripts/verify_catalogs.mjs`, `tests/test-catalog-filter-print.mjs` section 5) so
   the pattern and its known traps carry over, not just the button.
2. Add a Print action to the enlarged view for EVERY card class that can enlarge
   (proofs, book designs, elements — parity across the page; the operator named
   singles because that's what he was in). Printing from the enlarged view prints
   exactly that ONE design: image, design number (for singles the SERVED number `num`,
   never the file id — the s21 gate pins this), title + description where they exist.
   One page, casket-catalog-style presentation.
3. Known scars that apply, from the ledger (binding):
   - `scrollHeight-clientHeight===0` is BLIND under `overflow:hidden` — assert
     last-element-inside-page GEOMETRY on a true 816x1056 viewport, and a real
     Chromium PDF = 1 page, like the casket section-5 gate does.
   - A sabotage needle must hit the rule that APPLIES (specificity), and print modes
     are asserted in the print media direction AND the screen direction (no leakage
     into normal view or the other print paths — filter sheet, compare sheet, default
     print must stay byte-equivalent output; prove the default full-page print
     unchanged the way s14 D did: two pre-change captures for determinism, then
     compare).
   - Suites/gates NEVER overlap another run; check the port before trusting a served
     run; BW_BASE-pin the gate server.
4. Gate work: extend `scripts/verify_pcm_catalog.mjs` (315/0 today) — the enlarged
   view exposes the print control for each class, the printed sheet carries exactly
   one design + the right number/title, geometry asserts, at least 2 sabotages proven
   red/green. Extend `tests/test-pcm-catalog.mjs` if that's where the print suite
   belongs (326/0 today). Re-pin counts in your report.

## Definition of done

- Builder change committed, page rebuilt deterministically (x2 sha-identical).
- Gate green at new pin on a fresh pinned server; sabotages recorded.
- Full `npm test` green from the worktree (pin today: 2764/41 on the main tree;
  worktrees read −2 via test-contact-csv's wmp check — expected, record per-suite).
- Page syntax 1 block 0 errors; index.html byte-untouched.
- Renders saved to scratch/s21-d-renders/: enlarged view with the print control
  (one per card class), the printed one-design sheet, and a casket catalog's
  single-item print for side-by-side parity. Look at them.
- Report: what the casket pattern actually was, deviations, assert pin, anything
  for the operator's eyeball.
