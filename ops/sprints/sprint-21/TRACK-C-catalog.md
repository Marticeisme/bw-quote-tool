# TRACK C — s21/catalog — proofs to the top: two sections, dropdown/format/search integration

You are an Opus track agent in the BW Quote Tool sprint system. Branch `s21/catalog` in
your own worktree, based on main AFTER Tracks A, B1, B2 merged — verify all three are on
your base (data/pcm-single-proofs.json, data/pcm-desc-singles.json,
data/pcm-desc-companions.json, pcm-single-images/ with 373 files, pcm-companion-images/
with 254). Plus the stale-base check: your branch must contain local main's tip; if not,
STOP and reset. Always `git -C <absolute-worktree-path>`; explicit-path staging only.

**pcm-design-catalog.html is GENERATED by `scripts/build_pcm_catalog.py` — edit the
builder and data, rebuild the page, never hand-edit the page.** Do not touch index.html
or any guide. Rebuild must be deterministic (build twice, byte-identical).

## Operator rulings you are implementing (2026-08-07)

1. **Two proof sections at the TOP of the catalog** — Single Marker Designs (373) first,
   then Companion Designs (254) — ABOVE the book-plate design sections. These are the
   highest-quality, newest designs; a family opening the link sees them first. The
   existing bottom "Companion Design Proofs" section MOVES/BECOMES the companion section.
2. **No bespoke per-section filter UI.** Integration goes through the EXISTING controls:
   - The design-books dropdown (`<option value="">Both design books</option>`, ~line 362
     of the current page; find it in the builder) gains entries for both proof sets so a
     family can scope to "Single marker designs" / "Companion designs". Verify scoping
     actually filters cards AND category re-rendering stays consistent (read the jump-box
     and facet-filter comments around lines 9169/9341 first — there are known re-filter
     subtleties).
   - Format facets: singles carry `format: single`, companions keep `format: companion`.
     Typing "single"/"companion" in search reaches them (the existing facet-through-search
     path — s17 fixed a substring bug here; don't regress it, the gate pins it).
   - Search bar: titles + descriptions + language go into the search haystack (_hay).
     "vietnamese" must surface every language-tagged card. Jump box: proof numbers
     resolve (the existing designs-first-proofs-second ordering comment governs).
3. **Cards get real text**: title + one-sentence description from the B-track data files;
   subject tags render as the same tag chips the book designs use (same slugs = same
   categories, which is the point). **REMOVE the `source: full-colour proof` chip
   entirely** ("not helpful to a family"). Keep the format chip.
4. Section intro prose in the operator's voice (docs/GUIDES_VOICE_DEBRIEF_2026-08.md
   BINDING: no em dashes, no marketing copy; short, honest, first person). Explain what a
   full-colour proof is in one family-useful sentence, as the current bottom section does.
5. Compare / print participation: proofs already participate in Compare and the filter
   print sheet (12/page). Singles join the same machinery. Verify print counts/pagination
   still make sense ("Print these N · k pages") with ~1000 total cards.

## Gate work (this is half the track)

`scripts/verify_pcm_catalog.mjs` (currently 238/0, BW_BASE-pinned, fresh server on 4173):
update and extend. New asserts at minimum: section ORDER (both proof sections above the
first book section), counts 373/254, `source: full-colour proof` ABSENT from the page,
every card has non-empty title+desc, tag chips drawn from the shared vocabulary, the 12
released numbers present (2500…2529), the 230-repeat rule (each shipped number appears
EXACTLY once), dropdown options present and functional (drive the select, assert card
visibility), "vietnamese" search surfaces ≥ the language census count from the desc
files, jump box resolves a single and a companion number, existing book-design asserts
stay green. **Sabotage-prove at least three new asserts red/green** (e.g. reinject the
source chip via builder mutation → named FAIL; swap section order → named FAIL; empty a
desc → named FAIL). Re-pin the assert count in your report.

Old-gate maintenance scar (STATE s20, binding): update stale asserts to the SAME INTENT
at the new surface — never delete an assert to go green, never "fix" code backward to
satisfy a stale probe.

## Definition of done

- Builder + data changes committed by explicit path; page rebuilt twice byte-identical.
- verify_pcm_catalog green at the new pin on a fresh BW_BASE-pinned server; sabotages
  red/green recorded.
- Page syntax check green (node script-block extraction, same pattern as index.html's);
  index.html 8/0 and BYTE-UNTOUCHED (sha256 before/after).
- Full `npm test` GREEN from the worktree root — record per-suite totals; never overlap
  runs; never call Firebase save/persist.
- Renders eyeballed AND SAVED to scratch/s21-c-renders/: page fold (the new top),
  each new section, one Vietnamese-tagged card, dropdown scoped to each proof set,
  search "vietnamese", search "roses" mixing books+proofs, print sheet page 1.
- Report: assert pin, sabotage evidence, deviations, anything the operator should eyeball.
