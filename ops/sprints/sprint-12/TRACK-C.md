# TRACK-C — Guide print design: restore the brand, bleed the cream, kill the header, seat the footer

You are a track subagent in sprint-12 of the BW Quote Tool. Obey
`ops/SPRINT_GUIDELINES.md` and `ops/DESIGN.md`. Work ONLY in your worktree
(`git -C` absolute paths always). Branch: `s12/guide-print-design`. Commit locally with
`[s12/guide-print-design]` prefixes; NEVER push; NEVER write to Firebase.

## The operator's complaint (2026-08-03, verbatim + his screenshot of the Pre-Planning Guide PDF)

"I think our guides took a step back as well. It's missing all bonney watson logoes and
the margins are supposed to be stretched to the edge. we also do not need a header and
the footer should just overlap with the cream not sit outside separately. the guides do
not look good at all."

What his screenshot shows (page 1 of the Pre-Planning Guide PDF): a cream content block
floating inside wide white page margins; a lone "Bonney Watson" text running header at
top-right in the white margin; the footer line ("Martice Morrison · (206) 445-9794 ·
mmorrison@bonneywatson.com" + "Page 1 of 4") in the white margin BELOW the cream block;
no logo anywhere on the page.

## The four directives

1. **Bonney Watson logos back on the guides.** The masthead logo was print-hidden in an
   earlier round ("costs pages") — that decision is now REVERSED by the operator. The
   brand mark is the fleur logo with navy #466e86 / orange #e84610 (brand ref:
   GPL-April-2026.pdf; see docs/BRAND_AND_BUILD_LOG.md for tokens). Restore it
   tastefully — the title block / cover area of each guide PDF, small enough not to
   blow the page cap.
2. **Cream stretches to the page edge.** The cream background must be full-bleed: zero
   white frame around it. These PDFs are generated by headless Chromium — set @page
   margins to 0 and carry the content inset INSIDE the cream (keep readable text
   insets, ~0.5in equivalent, as padding on the cream, not as page margin).
3. **No running header.** Remove the "Bonney Watson" top-right running header entirely.
4. **Footer sits ON the cream.** The running footer (name/phone/email · Page X of Y)
   stays, but rendered on the cream surface at the bottom of the content — not in a
   separate white band outside it.

Plus the operator's general mandate: "the guides do not look good at all" — while you
are in there, fix obvious spacing/typography awkwardness page-wide, but do NOT change
content, prose, prices, or the price-rule system. Design only.

## Where this lives

- `scripts/guide-print-system/` — the generator (idempotent) that applies print
  structure to the 19 `*-guide.html` files; `guide-print.css` carries @page margins and
  the running footer. READ the system end to end before editing; it was built by Track
  P4 (see STATE.md "Guide-PDF A+ system" entry) with covers OFF per an s11 operator
  ruling — covers stay off, this is not a cover revival.
- PDF build: the guide PDF builder + `.build-manifest` (content-hash staleness gate).
  Chromium `page.pdf` — note the P4 scar: cover-photo CSS backgrounds need an explicit
  wait before pdf() or they render blank; if you move the logo/backgrounds around,
  respect that wait.
- Gates: `scripts/verify_guide_pages.mjs` (≤6 TOTAL pages per guide, no-full-bleed-page
  cover gate — your full-bleed CREAM is background, not a full-bleed photo page; make
  sure the gate distinguishes them rather than deleting the check), stranded-sheet
  gate, `verify_photo_first.mjs`, `verify_guides_page.mjs`, the family-register gate
  (`_no_mis_assert.mjs` 20-term) which runs via verify_guide_pages — all must stay
  green or be consciously amended with justification in your report.

## Constraints

- **Page caps hold: 6 total pages per guide.** If restoring the logo pushes any guide
  over, tighten vertical rhythm — do not cut content. Report per-guide page counts
  before/after.
- **Rebuild all 19 PDFs on your branch's bytes** and update `.build-manifest` — an
  incomplete rebuild is the classic staleness-gate failure at director merge.
- Screen rendering of the guides must not regress: the print system is @media print
  fenced. Verify at least 3 guides' screen views pixel-unchanged (or justify any diff).
- Do not touch: PCM files (two parallel tracks own them), catalogs, GPL, contract
  generators, index.html.
- Line endings are CRLF; edit accordingly.
- Worktree has no node_modules — junction the main repo's before running suites.

## Verify — look at your work

- Rasterize page 1 + one interior page of at least 6 guides (PyMuPDF or pdf.js) at
  before/after, side by side, into `scratch/s12c-renders/`. LOOK at them: logo present
  and crisp, cream to the edge on every side, no header, footer on cream, nothing
  clipped by the 0 margins, page counts within cap.
- Run: `npm run check` (8 blocks, 0 errors), full `npm test` in the worktree (record
  counts), verify_guide_pages / verify_photo_first / verify_guides_page ALL green.
- Sabotage-prove any gate change both directions; transcript in the report.

## Report

Deliver: what changed in the print system, per-guide page counts before/after, render
paths, gate amendments with justification, sabotage transcript, suite counts, honest
caveats. Your final text is the report — raw facts over polish.
