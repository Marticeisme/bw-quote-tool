# TRACK X — COM map layout accuracy from the operator's walkthrough video

Repo: `C:\Users\Martice\bw-quote-tool`; you run in the WORKTREE
`C:\Users\Martice\bw-quote-tool-comvid` on branch `s10/com-video-accuracy`
(node_modules junction in place). ALWAYS `git -C C:\Users\Martice\bw-quote-tool-comvid …`.
Obey `ops/SPRINT_GUIDELINES.md` + `ops/DESIGN.md`. Commit locally with explicit paths,
NEVER push; never write to production Firebase; never read `wmp-cemetery-map/`.

## Operator request (2026-08-01, verbatim intent)

"the chapel of memories mausoleum map is still not very accurate … watch the video in
the folder of my walkthrough of the chapel. you should be able to grasp the layout much
better as well as take names from the video to figure out where it is in relation to
the walls."

The map: `MAPS/COM_CryptMap.html`, generated by `scripts/build_com_map.mjs` from
`scripts/com-crypt-data.mjs`, gated by `scripts/verify_com_map.mjs`. Its geometry was
built from the CAD plan + still photos only; the original track skipped the video (no
ffmpeg, time budget). NOTE: sprint-09 reworked the COM builder — read the CURRENT
files and `git log --oneline -- scripts/build_com_map.mjs` first; do not assume the
sprint-08 shape.

## The video

`D:\Cemetery Photos Misc\Chapel of Memories\20260729_124129.mp4` (223 MB, ~2.5 min,
phone). No ffmpeg on PATH. Get frames with Python:
`pip install imageio-ffmpeg` (bundles an ffmpeg binary; `imageio_ffmpeg.get_ffmpeg_exe()`
gives you the path — run it directly for a frame every ~0.5–1 s into gitignored
`scratch/comvid-frames/`). Phone video may be rotated — check and correct. Then READ
the frames in sequence (batches of crops where detail matters) and reconstruct the
actual walk: where he enters, which way he turns, which banks face which hallway, how
wide the spaces feel, where the chapel/altar/pews actually are, where Radiance and
Serenity actually hang, ceiling/skylight, floor materials.

**Names as landmarks — explicitly authorized.** The operator ruled (2026-07-29/08-01)
that names appearing in his property media are fine, and he explicitly asked you to
use them: a name legible on a crypt front is a fixed physical landmark — the same name
seen in two segments proves those frames show the same wall, and name positions let
you chain overlapping views into a route map. Work with them freely in your gitignored
`scratch/` notes. Committed FILES and your report still use bank ids/refs, not names —
the map itself renders no names (unchanged), so none belong in code or data.

## What to change

GEOMETRY AND PLACEMENT ONLY, via the data module / builder (never hand-edit the HTML):
bank positions, facings, lengths; hallway widths; the island shape; chapel/altar/pew
area; Radiance/Serenity wall positions and facings; entrance orientation; materials/
colors if the video contradicts the current ones. **The inventory data — refs, tiers,
counts, statuses, prices — must be byte-untouched**; the gate's counts/status/price
anchors must pass unchanged, and your diff must show no edits inside the unit tables.

Sprint-10 §1 lists operator judgement calls the video should SETTLE — answer each with
frame evidence (frame timestamps in your report):
1. Chapel located at the CAD's "CHAPEL AREA" west of the island — is that where the
   pews/altar really are?
2. Seating faces north (a photo was readable as west) — which way do the pews face?
3. Both niche walls' facing directions were estimated — what does the video show?
4. Banks 154–158 kept in COM though the MIS debrief says ELM-3-S — does the walk show
   them inside this building or through a doorway in another?
Any the video cannot settle stays flagged — say so, don't guess.

## Verification gates (quote outputs verbatim)

1. `node scripts/verify_com_map.mjs` PASS with all inventory anchors IDENTICAL to
   before your change (quote the counts/status/price lines before and after);
   determinism intact; if the gate has geometry-specific assertions, update them
   deliberately and minimally, and say which.
2. Playwright: re-run/adapt the existing COM interaction checks (see the current
   `scratch/` or rewrite from the family patterns): taps, drag≠select, area
   navigation, print scope, zero page errors.
3. `npm run check` → 8 blocks 0 errors; `npm test` counts never fall from branch time
   (current contract: `1534 passed, 0 failed across 31 suites`; the port-3737
   worktree artifact on test-price-update-path is documented — don't kill the server,
   don't call main red).
4. Screenshots BEFORE and AFTER for: 3D whole building, floor plan, each area with
   changed geometry — plus, for each change, the video frame(s) that justify it
   (frame files stay in gitignored scratch; reference them by filename/timestamp).
   LOOK at everything.
5. A route narrative in the report: the walk reconstructed step by step
   ("enters at …, turns …, passes bank … on the left …"), so the operator — who walks
   this building daily — can falsify it line by line.

## Report

What changed and why (per change: video evidence by timestamp); the four judgement
calls answered or explicitly unsettled; verbatim gates; before/after screenshot list;
decisions & open questions.
