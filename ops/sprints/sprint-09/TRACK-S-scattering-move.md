# TRACK S — Scattering garden: off the maps, into the quotes (`s09/scattering-move`)

You are a track subagent. Read `ops/SPRINT_GUIDELINES.md` and `ops/DESIGN.md` (§6 and §7
especially) and obey them. Branch `s09/scattering-move` from a `main` carrying Tracks A
and U. Two surfaces:

- **`wmp-cemetery-map/`** — its own LOCAL-ONLY git repo (no remote; never pushed; never
  committed into the tool repo). ~29 scattering mentions in its `index.html`.
- **`index.html`** (tool repo) — only if a change is actually needed (see below).
  Browser-pane hazard: at most one Edit, then Node scripts.

## Operator instruction (Map Issues 07.31.26)

> "Scattering garden options should not be on the maps it should be with the quotes by
> the direct cremation options."

## What to do

1. **Map side:** find every place the WMP map presents scattering-garden *purchase
   options* (prices, "buy/quote this" affordances). Remove the sales/option presentation.
   The scattering garden as a *place* may remain on the map (it physically exists);
   what goes is offering it as a purchasable option there. Use judgement; if the map
   only shows it as a location with a price, strip the price. Commit in the MAP repo
   (`git -C C:\Users\Martice\bw-quote-tool\wmp-cemetery-map`), run the map repo's own
   `npm test` (19 passed / validate 2/2, 2770 units) before and after. **Never copy any
   real name or unit data from the map repo into the tool repo — not in code, fixtures,
   comments, or your report.**
2. **Tool side:** verify the quote tool offers scattering-garden placement **with the
   direct-cremation options** (the cremation quote area already has scattering
   dropdowns — `Scattering Garden` appears in `index.html`). If it's already there and
   priced, your tool-side change may be ZERO — verify and report where it lives and
   what it charges. If it's missing or priced differently than the map showed, fix the
   tool side following existing option patterns and say where the price came from.
   MIS is the pricing source of truth — never harvest a price from a rendered sheet.
3. If `data/prices.json` carries scattering entries (it currently has none), remember
   both copies are generated by `wmp-cemetery-map/scripts/build-prices.py` — change the
   source, rebuild both, never hand-edit.
4. **ADDED 2026-07-31 (operator ruling at Track U's merge): urn-garden O&C is $875,
   not $985.** The 06/2026 package sheet prints a flat $875 for every urn-garden ground
   placement; the operator confirmed "$875 — sheet wins". Update the SOURCE that
   `build-prices.py` reads so `OC:ground_inurnment` becomes 875 (find where the fee
   amounts live — likely a fee table in the map repo; change the source, never the
   output), rerun `build-prices.py` so BOTH copies (`wmp-cemetery-map/data/prices.json`
   and the tool's `data/prices.json`) regenerate together, and re-run
   `scripts/verify_urn_garden_ranges.mjs` in the tool repo — its divergence NOTE about
   875-vs-985 should disappear; extend it to assert the fee if it doesn't already.
   Check whether `OC:boulder_inurnment` ($1,425) has any sheet contradiction — the
   sheet is silent on boulders, so leave it. Tool-side tests that pin 985 (grep for it)
   must be updated deliberately, never fudged.
   **Operator clarification 2026-07-31 (mid-wave): the $875 applies ONLY to
   `OC:ground_inurnment`.** Normal-plot O&C is deliberately higher and must not move:
   lawn_single $1,535 / lawn_double_1st $2,085 / lawn_double_2nd $1,535 /
   mausoleum_entombment $1,205 / boulder_inurnment $1,425 / niche_inurnment $875
   (already) / niche_non_inurnment $375 all stay. Diff both regenerated prices.json
   copies and prove ground_inurnment is the ONLY changed key; pin the unchanged fees
   in the verifier.

## Verification (verbatim outputs)

- Map repo: `npm test` green before AND after; grep evidence that option/price
  presentation is gone (counts before/after).
- Tool repo: `npm run check` 8/0; `npm test` counts not falling; generator baseline
  14/14 IF `index.html` changed (scattering options are quote-side; if a baseline
  scenario moves, stop and report).
- Screenshot of the map's scattering area after the change and of the tool's
  direct-cremation scattering option in `scratch/s09s-renders/` — look at both.

## Report

Standard format; state plainly whether the tool side needed any change at all.
