# TRACK C — Eternal Light Mausoleum map (generated, geometry-first)

You are a track subagent in sprint-14 of the BW Quote Tool. Obey
`ops/SPRINT_GUIDELINES.md` and `ops/DESIGN.md`. Work in the worktree
`../bw-quote-tool-s14c` on branch `s14/elm-map`
(`git worktree add ../bw-quote-tool-s14c -b s14/elm-map`); junction node_modules from the
main tree. Commit locally with explicit paths; NEVER push; `git -C <abs-path>` always.

## Mission

Build `MAPS/ELM_CryptMap.html` as a GENERATED page (data module → build script → gate
with sabotages — the COM/ECL/GOMN pattern; read `scripts/build_com_map*` /
`scripts/verify_*_map.mjs` for house style, `scripts/map-movement.mjs` for camera feel).

**GEOMETRY-FIRST (operator ruling):** real layout, section labels, bank shapes,
cross-links — NO prices, NO statuses, NO invented inventory. "Ask us" wording on
selectable positions (s11 CONFIRM-chip pattern). Data module leaves obvious empty
price/status slots for the later MIS-export load.

## Context you must hold

The **Eternal Light Columbarium (ECL) is a room INSIDE this building** — it already has
its own live map (`MAPS/ECL_NicheMap.html`, generated by `scripts/build_ecl_map*`, gate
`verify_ecl_map`, $536,710 avail anchor). Your map is the CRYPT MAUSOLEUM around it. Do
NOT rebuild or re-model the columbarium's niches — represent the ECL room as a labeled
link zone. Caution from s13: the photo folder called "Crystal Niche" in the operator's
archives is NOT the ECL room (it's the old brass room); the MIS sheet's "Crystal Niches"
label below is its own feature inside ELM — establish from the video what the crystal
niches actually are, and whether they ARE the ECL room or a separate feature, and say so
in your report. If ambiguous, model as an unlabeled-inert structure and flag for ruling.

## Geometry sources (in order of authority)

1. **MIS overview transcription below** (director's read of the operator's screenshot;
   no on-disk PNG covers this building).
2. **Video frames**: `D:\Cemetery Photos Misc\Eteernal Light Maus\20260803_121735.mp4`
   (9:29, walks mausoleum + columbarium) + the five stills in that folder (NOTE: the
   2024-04-04 stills in that folder were previously identified as COM's Serenity wall —
   trust the 2026 video over old stills; verify each still before using it).

### MIS overview transcription (Eternal Light Maus, center building; COM to its west,
Terrace Garden Maus to its east)

- **Top exterior, labeled "Garden Mausoleum":** a row of ~8 tandem-crypt banks labeled
  1A–1I (MIS shows, left to right in the drawing: 1I, 1H, 1G, 1F, 1D, 1C, 1B, 1A — with
  "Tandem Crypts" noted at both ends; exact set/count from footage).
- **Below that, inside the north wall:** two long wall banks both labeled "ELM-W-1" —
  left segment positions 1–14, right segment 15–28 — flanking a central pink-highlighted
  "Pickel Wall **ELN-W-1**" (note the ELN prefix — likely a niche wall; transcribe the
  label as MIS gives it but verify character-level from footage if it appears).
- **Central block (the heart of the building):** perimeter banks around a cross-shaped
  interior: "ELM-3-W" and "ELM-4-W" along the top (mirrored text in MIS), "ELM-3-S"
  vertical on the left, "ELM-4-S" vertical on the right, "ELM-3-E" and "ELM-4-E" along
  the bottom, "ELM-4-N" vertical far right. Center: "CRYSTAL NICHES" with an elevation
  view (a numbered row 1–24 visible) plus pink niche clusters arranged in a plus/cross
  pattern around the center.
- **Bottom corners:** banks "ELM-P-C" (left, beside a Rest Room) and "ELM-P-D" (right,
  beside the other Rest Room).
- Pink highlighting on several clusters — meaning unknown; do NOT model as status.

## Links (the operator's explicit requirement)

- This map carries a clearly-visible link/button to the **Eternal Light Columbarium map**
  (`ECL_NicheMap.html`) — persistent nav affordance + the ECL room as a click-through
  link zone in the 3D scene.
- Add ONE minimal reciprocal anchor on `ECL_NicheMap.html` back to this map (smallest
  possible diff; gate-assert it; `verify_ecl_map` must stay green — its anchors
  21 avail/$536,710 must not move).
- "← Quote Tool" back button (href="../").

## Rules that bind you

- Statuses pattern-never-hue; no price on sold/occupied; exact dollars — encode the
  conventions even though this ship is unpriced.
- "MIS" never renders family-facing; wire `scripts/_no_mis_assert.mjs` into your gate.
- Never hand-edit generated HTML.
- Gate: structural assertions (banks present, labels, both link directions,
  no-price-anywhere assert) + ≥6 sabotages proven both directions.
- Don't touch index.html, guides, contract code, other maps (beyond the one ECL anchor),
  or anything Track A/B owns (walkthrough pages; TG/TGMP pages).

## Definition of done

- Data module + builder + gate + page committed; gate green (quote numbers);
  `verify_ecl_map` re-run green post-anchor; npm run check 8/0; npm test exact count +
  env-pinned command.
- Renders: overview, the central crystal-niches block, the ECL link zone — in
  `scratch/s14c-renders/`.
- Report: per-section geometry confidence, the crystal-niches identification, ELN-W-1
  reading, MIS-vs-footage conflicts, exact commands run.
